import {NextRequest,NextResponse} from "next/server";import {supabaseAdmin} from "@/lib/supabase-admin";import {isAdminAuthorized} from "@/lib/security";export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){if(!isAdminAuthorized(req.headers.get("x-pawtra-admin-key")))return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;const {data:o}=await supabaseAdmin.from("orders").select("*").eq("id",id).single();if(!o)return NextResponse.json({error:"Order not found"},{status:404});const {data:p}=await supabaseAdmin.from("previews").select("id,version_number,storage_path,label").eq("order_id",id).order("version_number");const previews=await Promise.all((p||[]).map(async x=>{const {data}=await supabaseAdmin.storage.from("previews").createSignedUrl(x.storage_path,900);return{...x,imageUrl:data?.signedUrl||null}}));const {data:r}=await supabaseAdmin.from("revision_requests").select("id,message,status,created_at").eq("order_id",id).order("created_at",{ascending:false});return NextResponse.json({order:{...o,previews,revisionRequests:r||[]}})}
